# include <stdio.h>
# include <string.h>
int main()
{
	int b;
	char s[1000];
	scanf("%s", s);
	scanf("%d", &b);
	int n = strlen(s);
	int d = 1;
	for (int i = 1; i < n; i++)
	{
		d = (d * 10 + s[i] - '0') % b;
	}
	printf("%d", d);

}